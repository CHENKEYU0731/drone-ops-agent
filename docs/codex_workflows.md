# Codex 后续工作流

可以把以下任务继续交给 Codex：

- 增加 PX4 ULog 只读解析支持。
- 增加 ArduPilot BIN 只读解析支持。
- 增加 MAVLink 遥测只读导入。
- 增加 SITL 仿真验证，但保持真实硬件隔离。
- 增加 PDF 报告生成。
- 增加 Web Dashboard。
- 增加工单系统或 CMMS 集成。
- 增加失败运行审计。
- 增加规则阈值配置文件。

每个任务都应先更新安全边界和测试，再实现代码。
