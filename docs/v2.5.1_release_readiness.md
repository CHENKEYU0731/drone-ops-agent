# v2.5.1 Release Readiness

## 自动测试

```bash
pytest
pytest tests/unit/test_input_safety_limits.py tests/integration/test_quality_gate_exit_codes.py tests/integration/test_dashboard_backend.py tests/integration/test_demo_package_script.py tests/integration/test_portfolio_showcase.py tests/unit/test_report_validation.py tests/unit/test_open_source_log_registry.py tests/unit/test_v2_5_1_adversarial_review_docs.py
```

## 发布验收

- 运行 `scripts/verify_release.ps1`，确认 clean venv、受约束安装、全量测试、wheel/sdist 和 CLI smoke test。
- 运行 `scripts/build_portfolio_showcase.py`，确认 2.5.1 manifest、无原始 `.ulg/.bin`、无本地仓库路径并校验 ZIP SHA-256。
- 确认 managed marker 伪造、symlink 输出、超大 JSON/log/Markdown、非有限 JSON 数值和非 PASS CLI 退出码测试通过。
- 确认 Dashboard HTML 不包含 `innerHTML` 数据渲染。
- 确认 GitHub Actions Python 3.11 / 3.12 success 且不再出现 Node 20 action deprecation。

## 安全边界

发布仍为 offline-only、advisory-only 和 human-review-required，不增加真实无人机、MAVLink command、飞控参数写入、仿真器启动、真实维修系统或 fleet platform 集成。
