# v2.5.1 - Final Adversarial Hardening

Date: draft

Release status: draft

## 主要变化

- 加固 managed demo/portfolio 目录删除和发布输出 symlink 边界。
- 为 JSON、CSV、Markdown、ULog/BIN 和 audit 数量增加资源上限。
- 移除本地 Dashboard bundle 渲染的 `innerHTML` 注入点。
- 所有质量门禁在非 PASS 时统一返回退出码 1。
- 所有 diagnosis、maintenance 和 audit 强制人工复核。
- 禁止开源 registry 自我声明真实飞行认证。
- CI 使用固定直接依赖约束和 Node 24 action major 的 commit SHA。
- 项目版本更新为 `2.5.1`。

## 安全边界

系统仍为 offline-only、advisory-only 和 human-review-required，不连接真实无人机、飞控、仿真器、维修系统或 fleet platform，不执行 MAVLink command 或任何真实控制/写入动作。
