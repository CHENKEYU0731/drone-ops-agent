# v1.0.0 - Stable Offline Ops Agent

Date: draft

Release status: draft

Release positioning: stable offline advisory baseline covering the accumulated
capabilities from `v0.1.0` through `v0.9.0`.

## 范围

v1.0.0 不是新增大功能版本，而是把此前从 `v0.1.0` 到 `v0.9.0` 形成的离线无人机运维
Agent 能力收成第一个可靠主版本。

本版本聚焦：

- CLI contract freeze
- schema contract freeze
- audit contract freeze
- release readiness checklist
- safety boundary regression
- 文档状态同步

## Stable Contract Inventory

新增文档：

- `docs/v1.0.0_stable_contract.md`

该文档冻结 v1.0 稳定契约，包括：

- 稳定 CLI 命令清单
- 稳定 JSON 输出清单
- 稳定报告输出清单
- 稳定 schema surface
- 稳定 audit contract
- offline-only / advisory-only 安全边界
- v1.0 之后仍属于 experimental / future extension 的能力

## v0.1.0 到 v0.9.0 能力收口

v1.0.0 覆盖并稳定以下能力线：

- 离线日志解析
- 异常检测
- 故障假设
- 维护建议
- Markdown / PDF 运维报告
- 报告证据链验证
- `evidence_index.json`
- `report_validation.json`
- 飞行前检查
- 状态监控回放
- PX4 ULog / ArduPilot BIN 离线解析适配
- offline/mock simulation validation
- simulation scenario matrix
- simulation operational rules
- work order drafts
- work order validation
- audit JSON
- release readiness checklist

## 安全边界

v1.0.0 继续保持离线建议系统定位。

本版本不包含：

- 真实无人机连接
- MAVLink command execution
- arm/disarm
- takeoff / landing / RTL
- mission execution
- motor start
- firmware upload
- flight-controller parameter writing
- 真实 PX4、ArduPilot、Gazebo、SITL 或外部仿真器启动或连接
- 真实 CMMS、Jira、飞书或企业微信 API 调用
- 自动派单
- 自动执行维护动作
- 真实、敏感或未经确认允许的二进制飞行日志

## Draft Readiness Checks

v1.0.0 发布前需要确认：

- `pytest` 全量通过
- 核心 CLI workflow 通过
- report validation 通过
- simulation validation 通过
- work order draft / validation workflow 通过
- report / PDF 输出不回归
- GitHub Actions Python 3.11 / Python 3.12 通过
- 安全边界扫描无真实控制能力
